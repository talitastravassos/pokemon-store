import styled from "styled-components";

export const Wrapper = styled.aside`
  width: 30rem;
  padding: 2rem;

  h2 {
    padding: 1rem 0;
  }

  @media (max-width: 700px) {
    width: 20rem;
  }
`;
